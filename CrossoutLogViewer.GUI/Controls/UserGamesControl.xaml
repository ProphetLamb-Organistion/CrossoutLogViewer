<UserControl x:Class="CrossoutLogView.GUI.Controls.UserGamesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CrossoutLogView.GUI.Controls"
             mc:Ignorable="d"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             BorderThickness="0"
             d:DesignHeight="450"
             d:DesignWidth="800"
             x:Name="gameView">
    <Grid>
        <local:UserOverview x:Name="UserOverview" Height="Auto"/>

        <GroupBox Header="Users"
                  Style="{DynamicResource GroupBox}"
                  Grid.Row="1">
            <Grid>
                <local:GameListFilter x:Name="GameListFilter"
                                      FilterChanged="RefreshGamesFilter"
                                      Grid.Row="1" />
                <Button Content="Open Users"
                        Style="{DynamicResource MahApps.Styles.Button}"
                        Click="OpenUsersClick"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Margin="0,0,5,0"/>
            </Grid>
        </GroupBox>
        <local:PlayerGamesDataGrid x:Name="DataGridGames"
                                   Grid.Row="2" />
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
    </Grid>
</UserControl>
